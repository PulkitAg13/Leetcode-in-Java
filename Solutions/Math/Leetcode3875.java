class Solution {
    public boolean uniformArray(int[] nums1) {
        return true;
    }
}
// simple fact if any odd in array minus of that odd with any even becomes odd and if no odd in array then its already all even so its always true
